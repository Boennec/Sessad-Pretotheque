

<div class="product-item text-center">
	<a href="{{ path('article', { 'name' : article.name}) }}"><img src="/uploads/{{ article.imageURL }}" alt="{{ article.name }}" class="img-fluid"></a>
	<h5>{{ article.name }}</h5>
	<span class="product-price">{{ (article.prix/ 100) |number_format(2, ',', '.') }} €</span>
</div>
